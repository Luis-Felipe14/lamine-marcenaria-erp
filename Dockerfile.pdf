# Servidor PDF (Puppeteer) - deploy separado do frontend Cloudflare
FROM node:20-bookworm-slim

RUN apt-get update && apt-get install -y \
    chromium \
    fonts-liberation \
    ca-certificates \
    --no-install-recommends \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /app

ENV NODE_ENV=production
ENV PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
ENV PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
ENV SKIP_PUPPETEER_INSTALL=true
ENV NODE_OPTIONS=--max-old-space-size=192

COPY package.json package-lock.json ./
RUN npm ci

COPY server ./server
COPY src/pdf ./src/pdf
COPY src/lib ./src/lib
COPY tsconfig.pdf-server.json ./

# Assets otimizados para PDF (logo ~25KB + header ~36KB) — visual preservado
COPY public/pdf ./public/pdf
COPY public/lamine-logo.svg public/lamine-monogram.svg ./public/
COPY public/lamine-background.png ./public/
COPY public/login/slide-1.svg ./public/login/

EXPOSE 10000

CMD ["npm", "run", "pdf:server:prod"]
